collectors:
  enabled: {{ prometheus_enabled_collectors }}
collector:
  service:
    services-where: "Name='DNS' OR Name='Kdc' OR Name='Netlogon' OR Name='NTDS'"
log:
  level: warn
telemetry:
  addr: ":9182"
  path: /metrics
  max-requests: 5
